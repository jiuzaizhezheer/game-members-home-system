name: CI

on:
  push:
    branches:
      - master
      - develop
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - master
      - develop

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    env:
      PIP_ONLY_BINARY: ":all:"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install PDM
        run: |
          python -m pip install --upgrade pip
          pip install pdm
      - name: Cache PDM virtualenv
        uses: actions/cache@v4
        with:
          path: |
            __pypackages__/
            .venv/
            ~/.cache/pip
          key: ${{ runner.os }}-py3.12-${{ hashFiles('pyproject.toml', 'pdm.lock') }}
          restore-keys: |
            ${{ runner.os }}-py3.12-

      - name: Install dev dependencies
        run: |
          pdm lock --refresh
          make dev-install
      - name: Install dev tools via pip (wheel-only)
        run: |
          python -m pip install --upgrade pip
          python -m pip install --only-binary=:all: pre-commit black==24.10.0 ruff==0.6.8 mypy==1.19.1

      - name: Run pre-commit
        run: |
          make pre-commit

      - name: Run checks
        run: |
          make check
